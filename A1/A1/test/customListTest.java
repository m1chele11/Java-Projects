import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
/*
public class PListTestTwo {

    // Create an instance of your custom list
    private PList<Integer> list;


    @Test
    public void testGetAtIndexZero() {
        // Initialize the list with elements
        list = makeList(10, 20, 30);

        // Check that get(0) returns the first element
        assertEquals(10, list.get(0));
    }

    @Test
    public void testGetAtIndexOutOfRange() {
        // Initialize the list with elements
        list = makeList(10, 20, 30);

        // Try to access an out-of-range index, should throw IndexOutOfBoundsException
        assertThrows(IndexOutOfBoundsException.class, () -> list.get(3));
        assertThrows(IndexOutOfBoundsException.class, () -> list.get(-1));
    }

    @Test
    public void testGetRecursive() {
        // Initialize the list with elements
        list = makeList(10, 20, 30);

        // Check that get() works recursively
        assertEquals(10, list.get(0));
        assertEquals(20, list.get(1));
        assertEquals(30, list.get(2));
    }

    @Test
    public void testGetOnEmptyList() {
        // Initialize an empty list
        list = makeEmptyList();

        // Try to get an element from an empty list, should throw IndexOutOfBoundsException
        assertThrows(IndexOutOfBoundsException.class, () -> list.get(0));
    }

    // Define a factory method to create your custom list with elements
    private CustomList<Integer> makeList(Integer... elements) {
        CustomList<Integer> result = new CustomList<>();
        for (Integer element : elements) {
            result.add(element);
        }
        return result;
    }

    // Define a factory method to create an empty custom list
    private CustomList<Integer> makeEmptyList() {
        return new CustomList<>();
    }
}

 */
